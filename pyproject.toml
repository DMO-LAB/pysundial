[build-system]
requires = ["setuptools>=42", "wheel", "cmake>=3.10", "pybind11>=2.6.0", "numpy"]
build-backend = "setuptools.build_meta"

[project]
name = "sundials_py"
version = "0.1.0"
description = "Python bindings for SUNDIALS solvers"
requires-python = ">=3.7"
dependencies = ["numpy"]